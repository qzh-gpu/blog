---
title: "88. 合并两个有序数组（数组 / 双指针）"
date: 2026-09-13
tags: [CodeTop, LeetCode, 数组, 双指针, 简单]
category: codetop
difficulty: 简单
leetcodeId: 88
excerpt: "从后往前双指针合并两个有序数组。"
---

# 88. 合并两个有序数组（数组 / 双指针）

## 题目

给你两个按非递减顺序排列的整数数组 `nums1` 和 `nums2`，另有两个整数 `m` 和 `n`，分别表示 `nums1` 和 `nums2` 中的元素数目。  
请你合并 `nums2` 到 `nums1` 中，使合并后的数组同样按非递减顺序排列。  
注意：最终合并后数组不应由函数返回，而是存储在数组 `nums1` 中。`nums1` 的初始长度为 `m + n`，其中前 `m` 个元素表示应合并的元素，后 `n` 个元素为 `0`，应忽略。`nums2` 的长度为 `n`。

## 示例

输入：`nums1 = [1,2,3,0,0,0]`, `m = 3`, `nums2 = [2,5,6]`, `n = 3`  
输出：`[1,2,2,3,5,6]`

输入：`nums1 = [1]`, `m = 1`, `nums2 = []`, `n = 0`  
输出：`[1]`

## 解题思路

采用**从后往前双指针**。因为 `nums1` 后面有 `n` 个空位，从后往前填充不会覆盖还未处理的元素。  
设置三个指针：`i` 指向 `nums1` 的最后一个有效元素（`m-1`），`j` 指向 `nums2` 的最后一个元素（`n-1`），`k` 指向 `nums1` 的最后一个位置（`m+n-1`）。  
每次比较 `nums1[i]` 和 `nums2[j]`，将较大的放到 `nums1[k]`，然后相应指针前移。  
如果 `nums2` 还有剩余，直接复制到 `nums1` 前面；如果 `nums1` 还有剩余，则无需处理，因为它们已经在正确位置。  
时间复杂度 `O(m+n)`，空间复杂度 `O(1)`。

## 解答

```java
class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int i = m - 1; // i 指向 nums1 中最后一个有效元素的下标
        int j = n - 1; // j 指向 nums2 中最后一个元素的下标
        int k = m + n - 1; // k 指向 nums1 的最后一个位置，用于从后往前填充
        while (i >= 0 && j >= 0) { // 当两个数组都还有元素时
            if (nums1[i] > nums2[j]) { // 比较两个末尾元素，将较大的放到 nums1 的末尾
                nums1[k--] = nums1[i--]; // 填入 nums1[i]，同时 i 和 k 前移
            } else {
                nums1[k--] = nums2[j--]; // 否则填入 nums2[j]，同时 j 和 k 前移
            }
        }
        while (j >= 0) { // 如果 nums2 还有剩余元素（nums1 已全部处理完）
            nums1[k--] = nums2[j--]; // 将 nums2 剩余元素依次复制到 nums1 前面
        }
        // 如果 nums1 还有剩余，无需处理，因为它们已经在正确位置
    }
}
```
